# ProjectVault Automation

The WebApp now has the unified notification center, unread badges, announcement receipts, responsive ad slot and versioned cache cleanup.

True server-side AI publishing, FCM push, and exact calendar-time monthly email require provider credentials/secrets (AI API key, Firebase service account/FCM setup, and an email provider). The source package should not contain those secrets.
